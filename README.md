dsv is just a simple illustration of how struct works with variables and how they are declared
struct types just shows the difference between a global and local varivable where the struct containin employees is a struct while the one with manager ia a local variable
